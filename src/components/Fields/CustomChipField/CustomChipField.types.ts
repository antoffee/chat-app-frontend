import { CustomFieldProps } from 'types/form';
import { CustomSelectableValue } from 'types/select';

import { CustomChipProps } from 'components/CustomChip';

export type CustomChipFieldProps = CustomFieldProps<CustomChipProps, CustomSelectableValue>;
